# Env cho Backend
# Domain của Frontend (Vercel) để cấu hình CORS
# VD: https://your-app.vercel.app
FRONTEND_URL=http://localhost:3000

# Port backend chạy (Render tự inject biến PORT, không cần đặt tay)
PORT=3001
